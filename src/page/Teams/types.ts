export type TSelectedTeam = {
  name: string;
  players: string[];
  logo: string;
  img1: string;
  img2: string;
  VKgroup: string;
};

export interface IBoopProps {
  scale: number;
  timing: number;
}
