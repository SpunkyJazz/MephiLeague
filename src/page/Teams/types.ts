export type TSelectedTeam = {
  name: string;
  players: string[];
  logo: string;
  img1: string
  img2: string
};

export interface IBoopProps {
  rotation: number
  scale: number
  timing: number
};